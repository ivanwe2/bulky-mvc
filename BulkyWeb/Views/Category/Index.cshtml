@model List<Category>

@*<partial name="_Notification" />*@

<div class="container">
	<div class="row pt-4">
		<div class="col-6">
			<h1 class=" text-light">Category List</h1>
		</div>
		<div class="col-6 text-end">
			<a asp-asp-controller="Category" asp-action="Create" class=" btn btn-primary">
				<i class="bi bi-plus-square-fill"></i> Create Category
			</a>
		</div>
	</div>
</div>
<table class="table table-bordered table-striped table-light">
	<thead>
	<tr>
		<th>
			Category Name
		</th>
		<th>
			Display Order
		</th>
		<th>
			Actions
		</th>
	</tr>
	</thead>
	<tbody>
		@foreach(var item in Model.OrderBy(m => m.DisplayOrder))
		{
			<tr>
				<td> @item.Name </td>
				<td> @item.DisplayOrder</td>
				<td class="text-center">
					<div class="w-75 btn-group" role="group">
						<a asp-controller="Category" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-primary mx-2 rounded-pill">
							<i class="bi bi-pencil-square"></i> Edit
						</a>

						<a asp-controller="Category" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger mx-2 rounded-pill">
							<i class="bi bi-trash"></i> Delete
						</a>
					</div>
				</td>
			</tr>
		}
	</tbody>
</table>